@model HuellitasVetWeb.Entidades.Categoria
@{
    ViewData["Title"] = "RegistrarCategoria";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (ViewBag.msj != null)
{
    <div class="alert alert-primary" role="alert" style="width: 365px; height: 80px;">
        @ViewBag.msj
    </div>
}

@using (Html.BeginForm("RegistrarCategoria", "Categoria"))
{
    <div class="d-flex justify-content-center align-items-center" style="height: 100vh;">
        <div class="col-xl-8">
            <div class="card">
                <div class="card-header">
                    <div class="row align-items-center">
                        <div class="col-8">
                            <h3 class="mb-0">Registrar Categoría </h3>
                        </div>
                    </div>
                </div>

                <div class="card-body">

                    @{

                        if (TempData["ErrorMessage"] != null)
                        {
                            <div class="alert alert-danger">
                                @TempData["ErrorMessage"]
                            </div>
                        }
                    }

                    <h6 class="heading-small text-muted mb-4">Información</h6>
                    <div class="pl-lg-4">
                        <div class="row">
                            <div class="col-lg-12">
                                <div class="form-group">
                                    <label class="form-control-label" for="input-email">Descripción: </label>
                                    @Html.TextBoxFor(m => m.Descripcion, new { @class = "form-control", @placeholder = "Escriba la categoria", @required = "required" })
                                </div>
                            </div>
                        </div>

                    </div>
                    <div class="d-flex justify-content-center mt-4">
                        <input style="background-color:#855CBA; color:white" type="submit" class="btn my-4" value="Registrar">
                    </div>
                </div>
                <div class="card-footer text-center py-3" style="background-color: #e9ecef;">
                    <a href="@Url.Action("ConsultarCategorias", "Categoria")" class="text-muted small">
                        Volver a categorías
                    </a>
                </div>
            </div>
        </div>
    </div>
}

@model HuellitasVetWeb.Entidades.Servicio
@{
    ViewData["Title"] = "Agregar Servicio";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (ViewBag.msj != null)
{
    <div class="alert alert-primary" role="alert" style="width: 365px; height: 80px;">
        @ViewBag.msj
    </div>
}

@using (Html.BeginForm("AgregarServicio", "Servicio", FormMethod.Post, new {enctype = "multipart/form-data"}))
{
    <div class="d-flex justify-content-center align-items-center" style="height: 100vh;">
        <div class="col-xl-8">
            <div class="card">
                <div class="card-header">
                    <div class="row align-items-center">
                        <div class="col-8">
                            <h3 class="mb-0">Registrar Servicio </h3>
                        </div>

                    </div>
                </div>

                <div class="card-body">

                    @{
                        if (TempData["SuccessMessage"] != null)
                        {
                            <div class="alert alert-success">
                                @TempData["SuccessMessage"]
                            </div>
                        }
                        if (TempData["ErrorMessage"] != null)
                        {
                            <div class="alert alert-danger">
                                @TempData["ErrorMessage"]
                            </div>
                        }
                    }

                    <h6 class="heading-small text-muted mb-4">Información</h6>
                    <div class="pl-lg-4">
                        <div class="row">
                            <div class="col-lg-12">
                                <div class="form-group">
                                    <label class="form-control-label" for="input-username">Descripción del servicio</label>
                                    @Html.TextBoxFor(m => m.Descripcion, new { @class = "form-control", @placeholder = "Ingrese la descripción", @required = "required" })
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-lg-6">
                                <div class="form-group">
                                    <label class="form-control-label" for="input-email">Precio</label>
                                    @Html.TextBoxFor(m => m.Precio, new { @class = "form-control", @placeholder = "Ingrese el precio", @required = "required", @type = "number" })
                                </div>
                            </div>
                            <div class="col-lg-6">
                                <div class="form-group">
                                    <label class="form-control-label" for="imagen">Imagen</label>
                                    <input type="file" id="ImagenServicio" name="ImagenServicio" class="form-control form-control-user" required />
                                    
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="d-flex justify-content-center mt-4">
                        <input style="background-color:#855CBA; color:white" type="submit" class="btn my-4" value="Registrar">
                    </div>
                </div>
                <div class="card-footer text-center py-3" style="background-color: #e9ecef;">
                    <a href="@Url.Action("ConsultarServicios", "Servicio")" class="text-muted small">
                        Volver a servicios
                    </a>
                </div>
            </div>
        </div>
    </div>
}

